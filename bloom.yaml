environments:
  default:
    shutdownPostDebugSession: true

    tool:
      name: "atmel-ice"

    target:
      name: "attiny1614"
      physicalInterface: "updi"
      hardwareBreakpoints: true

    server:
      name: "avr-gdb-rsp"
      ipAddress: "127.0.0.1"
      port: 1442
      rangeStepping: true

insight:
  activateOnStartup: false
