environments:
  default:
    debugTool:
      name: "atmel-ice"

    target:
      name: "attiny1614"
      physicalInterface: "updi"

    debugServer:
      name: "avr-gdb-rsp"
      ipAddress: "127.0.0.1"
      port: 3333

insight:
  enabled: true
